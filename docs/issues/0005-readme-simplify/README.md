# Issue #5：项目 README.MD 简化

- Issue：<https://github.com/zhshenry/To-Do-List/issues/5>
- 建档：2026-09-21
- 状态：已归档
- 类型：文档
- 涉及UI：否
- 分支：sdd/0005（worktree `../To-Do-List-sdd/0005`）

## 背景（issue 要点 + 勘察结论）

**用户诉求**（issue 原文）：README 内容太多字太多，简单保留主要功能、如何运行、版本历史链接等信息就好。

**勘察**（2026-09-21，main `499992f`）：README.md 现 61 行、五个大段；「日常使用」为长 bullet 段、「AI 设置和隐私」三大段近全文最重；**下载段写死 `0.5.1` 版本号已过时**（当前 0.5.6）；「悬浮入口（暂时下线）」为历史性段落。

## 方案（明确的删留映射，无开放决策点）

新 README 目标 ≤30 行，结构如下，逐段映射：

| 新结构 | 内容来源 |
|---|---|
| 标题 + 一句话定位 | 现首段压缩（Electron + React + TS 悬浮待办/AI 助手；内置 SQLite，无需装 Node.js） |
| `## 主要功能`（4 条 bullets） | 从「日常使用」「AI 设置」提炼：①悬浮卡+主界面（今日待办/日程/逾期提醒/稍后10分钟）②待办/日程/标签/优先级/进度 + 事项库搜索恢复 ③AI 助手可选（自然语言记录、确认后写入、本机存储无遥测）④托盘常驻/自启/JSON 备份/自动更新 |
| `## 下载与运行`（4 条） | 压缩现下载段：安装版（Releases 下载 Setup，内置自动更新）；免安装版（Portable zip 解压运行，升级整体替换）；数据在 `%APPDATA%/To-Do-List` 升级卸载不清；Win x64 实测 Win11、无签名提示。**不写死版本号，一律链接 Releases** |
| `## AI 与隐私`（2 条） | 三大段压缩：设置→AI 填供应商/Key/模型；密钥 Windows 加密；每次对话上传输入+最近对话+事项摘要到所配模型服务；数据本机 SQLite、无自有云无遥测 |
| `## 开发者`（1 行） | 保留 DEVELOPMENT.md 链接 |
| `## 版本历史`（1 行） | 保留 CHANGELOG.md + Releases 链接 |

**删除**：日常使用操作细节（快捷键、动画时长、交互细节约 20 行）、AI 对话交互细节段、悬浮入口（暂时下线）段、数据与备份细节段（保留一句数据目录）。**删除内容以 git 历史为存档，不另建文档**。

## 决策记录

- 2026-09-21 建档：spec 按删留映射表执行，无开放决策点（用户诉求"等信息就好"已足够明确；精简幅度以 ≤30 行为硬指标）。
- 2026-09-21 spec 门：HUMAN_REVIEW（S1=0.91/S4=0.93 过，S2=0.65/S3=0.75 复核带）。
- 2026-09-21 **批准**（聊天，用户「approve」）：按删留映射表执行；#4 合并后的新 main（fd09eac）为基线。
- 2026-09-21 实现期 RETRY 一次：accept 门前置的 build 暴露 #4 遗留测试夹具类型错误（completedAt 不在 TaskInput），修复后全绿——RETRY 机制首次实战生效。
- 2026-09-21 accept 门 v2：HUMAN_REVIEW（A2=0.78/A5=0.31/A6=0.84）——A5 命题与文档类不匹配，记校准议题。
- 2026-09-21 **验收通过 + 预合并**（GitHub 评论 `/approve`，评论 id 5761862350，经 issue-commands.mjs 代码验证）：哈希重验通过 → `_integrate` 试合 + npm test 34/34 → 树哈希一致（`9ac9a05`）→ 主干合并 `204354e` → 合并后冒烟 passed → 分支/工作树清理 → 关单归档。本单实现期经历一次真实 RETRY（类型错误回炉），为 RETRY 机制首例。

## 实现要点

- 仅改 `README.md` 一个文件；无代码/测试影响。
- 验收（可测）：行数 ≤30；含 Releases / CHANGELOG.md / DEVELOPMENT.md 链接；不含写死的 0.5.x 版本号；含上述六个 H2/标题结构；链接均有效。
- CHANGELOG 记 [未发布] 一条。

## 验收记录

- **交付 commit**：`3f64027` @ `sdd/0005`（基线 main `fd09eac`），3 文件 +20/−50：`README.md` 重写为 30 行六段式（定位一句话 / 主要功能 4 条 / 下载运行 4 条 / AI 与隐私 2 条 / 开发者 / 版本历史；含 Releases/CHANGELOG/DEVELOPMENT 链接，无写死版本号）、`CHANGELOG.md`、`tests/store.test.ts`（**顺手修复 #4 遗留的夹具类型错误**——completedAt 不在 TaskInput，改标注 Task，tsc 因此前的提交未复跑 build 而漏网）。
- **验证**：`npm test` 34/34 · `npm run build`（tsc 类型检查）· `npm run test:desktop` passed。README 恰 30 行，达标 ≤30。
- **Jev accept 门 v2**：`HUMAN_REVIEW`——A2=0.78、A5=0.31、A6=0.84 全落复核带。**A5 偏低是命题与文档类不匹配**（"新增测试覆盖根因"对纯文档变更无意义），属策略缺口非交付缺陷——已记入对账议题：建议 policy 为文档类配置专用命题（如"链接均有效且无过时版本号"）。按 fail-closed 原则不临时改规则，交人工验收。
- **新 README 全文**见本档案同目录交付（git 历史 `3f64027`）；验收要点：六段结构、30 行、链接有效、无 0.5.x 写死。
- **验收方式（严格制）**：评论 `/approve` 或聊天说验收通过 → 触发第 6 节预合并收尾。
