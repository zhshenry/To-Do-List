## 编码行为准则（强制遵守）

> 本节用于减少 LLM 在编码过程中的常见失误。**这些规则倾向于谨慎而非速度**；对于明显简单的任务可用判断力放宽，但凡是非平凡改动均必须遵守。

### 1. 先思考，再编码 —— 不假设，不藏疑问，亮出权衡

实现前必做：

- 明确写出假设。不确定就直接问，不要默默猜。
- 存在多种解读时，把候选方案列出来让用户选，禁止"我替你选了一个"。
- 存在更简单方案，要明说，必要时反推用户决策。
- 遇到不清楚的地方，先停下来，指出哪里困惑，再问。

### 2. 极简优先 —— 只写解决问题的最小代码

- 不实现用户没要求的功能。
- 不为单点使用的代码搞抽象。
- 不加未被要求的"灵活性 / 可配置性"。
- 不为不可能发生的场景写错误处理。
- 如果写了 200 行但 50 行就够，**重写**。

自问："一个资深工程师会不会觉得这写得过于复杂？" 答案是 → 简化。

### 3. 外科手术式改动 —— 只动必须动的，只清自己留下的烂摊子

修改既有代码时：

- 不"顺手优化"周边代码、注释、格式。
- 不重构没坏的东西。
- 匹配既有风格，哪怕你觉得有更好的写法。
- 发现无关的死代码 → 提一嘴，不要删。

你的改动产生孤儿时（你新增/修改才导致未使用的）：

- 删掉因你的改动而失效的 import / 变量 / 函数。
- 不要顺带删除原本就存在的死代码（除非用户明确要求）。

校验标准：每一行变更都能直接追溯到用户的需求。

### 4. 目标驱动执行 —— 定义可验证的成功标准，循环直到通过

把任务转换成可验证目标：

- "加校验" → "为非法输入写测试，再让测试通过"
- "修 Bug" → "先写一个能复现 Bug 的测试，再让它通过"
- "重构 X" → "确保重构前后测试都通过"

多步骤任务先给出简短计划：

```
1. [步骤] → 验证：[检查点]
2. [步骤] → 验证：[检查点]
3. [步骤] → 验证：[检查点]
```

强成功标准让你能独立闭环；弱标准（"让它能跑"）会逼用户反复澄清。

---

这些准则生效的标志：diff 中无关改动变少 / 因过度复杂导致的返工变少 / 澄清问题出现在动手之前而不是出错之后。