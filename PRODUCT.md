# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

此处为 Impeccable 的渲染技术分类（Electron 内嵌 Web UI），不是浏览器发行平台；产品仅发布 Windows 桌面版。

## Users

面向其他 Windows 用户发布的桌面产品；作者也是日常用户。无账号、无角色，本机单人使用。

主要情境是人坐在 Windows 工作桌面前：需要随手记下事项、随时看见下一件该做的事、在到期时收到本机提醒，并用对话把含糊的日程说清楚后再写入。中文界面是产品语言，不据此推断地域市场或组织规模。

## Product Purpose

To Do List 是 Windows 上的悬浮待办与 AI 日程助手。它让事项留在桌面视线里，而不是藏进浏览器标签或必须打开的清单应用。

成功意味着：用户能快速记下待办或日程；主界面始终能回答「下一件是什么」；到期能收到本机通知；也能在独立对话里把安排说清楚，确认后才改本地数据。清单与确认后写入的 AI 同等核心，不能互相让位。

## Positioning

邻近的云待办或通用聊天工具不能如实复制的机制是：Windows 桌面上的清单与可独立常驻的圆形悬浮入口，以及可选的对话助手——模型可以追问和解释，但事项与标签的任何写入都必须先预览、再由用户确认，不能执行模型返回的代码。数据与密钥留在这台电脑上，不提供应用自己的云账号或同步。

## Operating Context

- 以 Electron 打包的 Windows x64 桌面应用运行；当前实测 Windows 11。用户不需要安装 Node.js、Python 或 SQLite。不提供 macOS 或手机构建。
- 默认分发免安装 ZIP（解压后运行 `To Do List.exe`，须保留整个目录）；可选 NSIS 安装包提供开始菜单、快捷方式与卸载。安装包内置向 GitHub Releases 检查更新；免安装版需手动替换目录。安装包尚未代码签名，Windows 可能提示发布者未验证。
- 数据在 `%APPDATA%/To-Do-List` 的本地 SQLite（`tasks.db`）。任务栏、托盘、`Ctrl + Shift + Space`（可被占用）、窗口置顶、登录启动、系统通知都是使用方式的一部分。点右上角「最小化」只隐藏主界面，不影响悬浮入口或 AI 对话，也不退出；托盘「退出（停止提醒）」才结束进程。关机、睡眠和应用退出期间不能发通知，启动或唤醒后补查未送达提醒。Windows 勿扰或通知权限可能挡住弹出。
- 主窗与圆形悬浮入口各自显隐、独立记忆位置和置顶；入口悬停或聚焦后可打开待办、AI 对话和切换图标置顶。设置与托盘可关闭或找回入口。内置圆环、小笺、嫩芽、团猫四款外观，默认圆环，也可使用自定义图片。AI 是独立、非模态的对话气泡窗口，主窗隐藏后仍可继续对话。
- AI 默认关闭。用户在设置中添加自己的模型供应商与 API Key 后，应用通过 Pi 循环访问当前选用的模型；不提供应用自己的模型服务或遥测。模型费用由服务方收取。

## Capabilities and Constraints

已具备：待办与日程（日程必须有时间，待办时间可空、可设优先级）；自定义标签（每项最多一个）；本地搜索；软删除与从已删除恢复；计划日期/时间与可选提醒（准时或提前 10 分钟）、稍后 10 分钟；未完成事项跨日仍可见且不改原截止时间；设置（通用 / AI配置）；JSON 导出备份（不含密钥），无 JSON 导入入口；最多 8 个供应商、每个最多 8 个模型；OpenAI Chat Completions、OpenAI Responses、Anthropic Messages；HTTP/HTTPS（远程 HTTP 为明文风险，仅建议可信内网或本机）。

主窗布局：保留用户确认的极淡暖冷网格渐变、日期标题、今日大容器卡和单张「后续事项」卡（含明天、近期、更晚分组）；默认紧凑列表，可切换磁贴。事项显示色点与标签名称，颜色不是唯一信息。日期右侧为「事项库」「新增」文字入口；展开态新增沿用小卡片的紧凑结构，标题可直接创建，时间、优先级、标签和更多设置按需展开，切换时弹窗宽高不变。新增时间与小卡片共用单列半小时档位。事项库独立切换主窗内容，提供跨日期搜索、标签筛选、未完成/已完成/已删除、恢复及批量完成/恢复。今日视图不放搜索，不恢复底部文字工具条。

硬约束（已确认，后续不得默默改掉）：

- 只做 Windows 本机：保留 Electron 与本地 SQLite；无账号、云同步、应用遥测；不做 macOS 或手机。
- AI 对事项或标签的变更必须先预览再确认，不能直接执行代码或 Bash。失败或取消保留用户输入。密钥用 Windows 系统加密，不进入渲染进程或事项备份；事项正文不做数据库加密。
- 产品语言是中文；口吻简短、实用，空状态可以略温暖。

当前未提供：重复事项、JSON 导入、多设备。AI 历史、输入草稿、可见工具过程保存在本机 SQLite，退出后保留；新对话确认后保留旧会话，可从历史选择。重启会将未完成的请求中断、未应用建议标记失效，不自动重放。

## Brand Commitments

- 产品名是 **To Do List**。npm 包名 `to-do-list` 与 appId `com.zhshenry.todolist.app` 是内部技术标识，不作为界面名称展示。
- 左上角圆环红点标识为用户提供的品牌资产（源图 `public/brand-logo.png`，界面绘制为 `BrandMark`）；应用、任务栏、托盘与通知使用已确认的第 3 版米白圆角图标（`assets/icon.png`、`assets/icon.ico`）。用户可在设置中换成自己的圆形裁切图片，默认图标仍保留可选。
- 界面文案用中文；事项类型统一使用「待办 / 日程」，所有入口、表单与反馈保持同一命名；不把开发说明、设计提示词或伪造的生产事项写进用户界面。

## Evidence on Hand

- 产品说明：`README.md`、`docs/PORTABLE-README.txt`。
- 已实现的交互与设计记录：`UX-CONTRACT.md`、`DESIGN.md`、`design-qa.md`。
- 标识与图标：`public/brand-logo.png`、`assets/icon.png`、`assets/icon.ico`。
- README 仍提到 `docs/approved-design.png` 与 `docs/approved-ai-floating-design.png`；当前仓库的 `docs/` 中这两份文件不在。无用户证言、媒体报道、基准测试或定价材料。后续工作不得编造客户、评价或未经验证的平台兼容声明（Windows 10 / ARM64 尚未实测）。

## Product Principles

1. 桌面上的「下一件事」和确认后才写入的 AI 同等重要；主清单不能被对话替换，助手也不能降成装饰入口。
2. 先把 Windows 单机记录、提醒和本机数据所有权做对，再考虑别的平台或云。
3. 模型可以追问和解释，不能悄悄改用户的事项。
4. 对要发给别人的 Windows 用户说话：中文、可独立理解、不依赖作者口头补充。
5. 不把未证实的能力（签名安装包、全平台、加密数据库、云同步）写成产品事实。
